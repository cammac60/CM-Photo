

const Image = async props => {
    const { id, url, caption, variantSize } = props;
    return (
        <div id="towers-wrapper" className="gallery-images-wrapper">

        </div>
    );
};

export default Image;